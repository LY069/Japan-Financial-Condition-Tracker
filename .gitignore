__pycache__/
*.pyc
.venv/
venv/
.env
*.tmp
.DS_Store
# Keep the built artifacts tracked (data/jfct.db, web/data.json, Japan_FCI_Tracker.xlsx)
# so the dashboard and workbook work straight from a clone.
